<idea-plugin>
    <!-- Reverse-domain id under a namespace the author controls. Note this differs
         from the Kotlin package (com.claudecode.chatplugin), which is fine. -->
    <id>io.github.repozitorro.claudebrains</id>
    <name>Claude Brains</name>
    <vendor url="https://github.com/repozitorro/claude-brains">repozitorro</vendor>

    <description><![CDATA[
        Claude Brains — a chat UI front-end for the Claude Code CLI, right inside
        your JetBrains IDE (WebStorm, IntelliJ IDEA, PyCharm, etc). Wraps the
        <code>claude</code> CLI as a subprocess instead of relying on a bare terminal:
        <ul>
            <li>Streaming chat panel with markdown rendering</li>
            <li>Multiple named, parallel sessions</li>
            <li>Slash-command autocomplete</li>
            <li>Model selector</li>
            <li>Send selected code / whole file to Claude with one action</li>
            <li>Floating prompt popup near the caret</li>
        </ul>
        Requires the Claude Code CLI to be installed separately and available on
        PATH (or configured with a full path in Settings). It runs the CLI as a
        subprocess and reuses its existing login, so no API key is needed.
    ]]></description>

    <change-notes><![CDATA[
        <b>0.6.0</b>
        <ul>
            <li><b>Chats no longer disappear when the IDE closes.</b> Shutting
                WebStorm down was being mistaken for closing the tabs by hand,
                so some conversations were deleted on the way out — a different
                few each time. Already-lost chats can't be brought back, but no
                more will go.</li>
            <li><b>Rename a chat</b> from the tool window's toolbar. "Chat 3"
                stops meaning anything by the third one.</li>
            <li><b>Stepping between changes now crosses files.</b> The next /
                previous buttons walked one file and wrapped inside it; a turn
                usually changes several, so they carry on into the next changed
                file and say how many are left.</li>
            <li><b>Accept is green, reject is red</b> — inside a change, on the
                strip over the editor, and above the prompt.</li>
            <li><b>Reject all asks first</b>, saying how much goes back and that
                Undo can return it.</li>
        </ul>

        <b>0.5.2</b>
        <ul>
            <li><b>Stepping between changes no longer throws.</b> Drawing a
                change asked the platform for its document in a way that is only
                allowed inside a read action, and painting doesn't run in one —
                so every repaint of a change reported an error. The document is
                held directly now. The fault was reachable only once the
                next / previous buttons started working in 0.5.1.</li>
        </ul>

        <b>0.5.1</b>
        <ul>
            <li><b>A turn that needed permission no longer hangs the chat.</b>
                The CLI was asking for confirmation and waiting for an answer
                this panel has no way to send, so the turn never finished — no
                reply, no error, just a chat stuck thinking. It now can't wait,
                and says which tools were blocked and how to allow them.</li>
            <li><b>Accept and reject are green and red.</b> They were told apart
                only by reading them, and reject wore the same red as the
                deleted lines behind it.</li>
            <li><b>The strip's next / previous buttons work.</b> They were
                drawn where you could see them but were not laid out, so a
                click passed straight through — and the caret they moved was
                invisible in an unfocused editor, which looked like nothing
                happening.</li>
            <li><b>The limits readout is legible.</b> It was pinned to an
                absolute 10pt, so it ignored the IDE's font size and display
                scaling entirely, in the palette's <i>disabled</i> grey. It now
                follows the interface font, turns amber past 75% and red past
                90%, and the bar under it is no longer a hairline.</li>
        </ul>

        <b>0.5.0</b>
        <ul>
            <li><b>A reply can no longer put live HTML or scripts into the chat
                panel.</b> Markdown was rendered with raw HTML passed straight
                through, so anything Claude quoted — a file, a web page, the
                output of a tool — could introduce working markup into a panel
                that holds a bridge back into the IDE. It is escaped now.</li>
            <li><b>Closing a chat stops the turn it was running.</b> The CLI
                process used to carry on with nowhere left to report to.</li>
            <li><b>Closed chats are actually released.</b> Every one used to stay
                in memory for the life of the project, and its usage readout kept
                ticking once a minute even after the project was closed.</li>
            <li><b>Usage Statistics opens where the embedded browser is
                missing</b>, the way the chat itself has since 0.4.5 — it was the
                one place left that threw instead of falling back.</li>
            <li>The usage poll can no longer leave stuck <code>claude</code>
                processes behind. Its timeout could never be reached, so a run
                that did not return was never cleaned up — once a minute,
                indefinitely.</li>
            <li>Under the hood: reading the CLI's protocol is now separate from
                running it, and covered by replaying a recorded turn, so a change
                in the CLI shows up as a failing test rather than as a chat that
                renders nothing. Every build is also checked against the
                platforms the plugin says it supports.</li>
        </ul>

        <b>0.4.5</b>
        <ul>
            <li><b>Fixes the panel coming up empty on WebStorm 2026.2</b>, where the
                embedded browser moved into its own bundled plugin: touching it
                threw, and that took the whole tool window with it. The dependency
                is declared now, and a missing browser costs only the rich
                renderer — the chat falls back to its Swing view.</li>
        </ul>

        <b>0.4.4</b>
        <ul>
            <li><b>Accept / Reject sit next to the change</b> they belong to, instead
                of at the far right of a wide editor</li>
            <li><b>Deleting a chat always asks first</b> — closing the tab used to
                discard the conversation silently. The button wears a trash can now.</li>
            <li>A thin bar under the limits shows how full the session window is,
                shifting from amber to red as it fills</li>
        </ul>

        <b>0.4.3</b>
        <ul>
            <li><b>Context window readout</b> — how full the model's context is for
                this conversation, so it's clear when it is heading for a
                compaction</li>
            <li><b>Delete a chat</b> deliberately, with a confirmation that says what
                is lost. The CLI's own transcript on disk is left alone.</li>
            <li>Buttons everywhere now take a hand cursor, including the painted
                Accept / Reject inside a change</li>
        </ul>

        <b>0.4.2</b>
        <ul>
            <li><b>Inline review actually engages now.</b> It had required the
                editor's document to match a separately-read disk snapshot
                character for character, and any divergence silently disabled it.
                The pre-edit content is reconstructed from the document itself
                instead, and it survives unrelated later edits.</li>
            <li>Changed files open whether or not they can be marked up.</li>
            <li><b>Accept / Reject moved to the right edge of each change</b>, and a
                strip floats over the bottom of the editor to step between changes
                and take the whole file at once.</li>
            <li>Limits moved onto their own line under the model selectors; the
                composer no longer repeats the reset countdown.</li>
        </ul>

        <b>0.4.1</b>
        <ul>
            <li><b>Real usage percentages</b>, refreshed every minute — session and
                week, taken from the CLI's own <code>/usage</code> report, beside the
                reset countdown. Costs nothing and pauses while the panel is hidden.</li>
            <li><b>Inline review and revert now work on CRLF files.</b> The CLI
                reports its edits with plain newlines even when the file uses CRLF,
                so on Windows projects nothing matched and both features silently
                declined to run. Reverting also keeps the file's own line endings
                instead of rewriting every line.</li>
        </ul>

        <b>0.4.0</b>
        <ul>
            <li><b>Review Claude's edits in the file.</b> When a turn ends the
                changed files open with new lines highlighted, the lines they
                replaced shown in red above them, and <b>Accept / Reject on each
                change</b> — in the gutter and inline. A bar above the prompt
                counts what's left and offers <b>Accept all / Reject all</b>.
                Rejecting is an ordinary undoable edit.</li>
            <li><b>Rate-limit readout</b>: which window you are in, a ticking
                countdown to its reset, and the tokens spent inside it. Claude
                Code never publishes a remaining quota, so this shows real
                consumption rather than an invented percentage.</li>
        </ul>

        <b>0.3.5</b>
        <ul>
            <li>Pasting a screenshot into the prompt, properly this time. The
                previous attempts hooked places the shortcut never reaches: inside
                the IDE, Ctrl+V is taken by the global paste action, which writes
                into the field directly. Image paste is now an action registered
                on the prompt itself, which outranks the global one, and it picks
                up whatever shortcut your keymap uses for paste.</li>
        </ul>

        <b>0.3.4</b>
        <ul>
            <li>A proper icon: a top-down brain in line art, replacing the
                placeholder</li>
            <li><b>Pasting a screenshot into the prompt now works.</b> It was
                bound to Ctrl+V, which the IDE claims for its own paste action,
                so it usually did nothing; it now goes through the text area's
                transfer handler — and dragging an image in works too</li>
            <li>A larger, roomier prompt font</li>
        </ul>

        <b>0.3.3</b>
        <ul>
            <li>An expired login is now actionable: the failed turn shows a
                <b>Sign in</b> banner instead of only an error, and
                <code>/login</code> starts the sign-in rather than reporting
                itself unavailable. The CLI reports a stale login as valid, so
                this can only be caught when a request fails.</li>
        </ul>

        <b>0.3.2</b>
        <ul>
            <li>Fixes the tool window opening empty ("Nothing to show") in 0.3.1:
                building the panel threw before any tab was added</li>
            <li><b>Sign-in screen</b> when the Claude Code CLI isn't signed in,
                instead of a chat that fails on the first prompt. Signing in runs
                the CLI's own <code>auth login</code> in a terminal — no
                credentials are entered into the plugin. A missing CLI gets its
                own message and install hint.</li>
        </ul>

        <b>0.3.1</b>
        <ul>
            <li>Pick a <b>specific model</b> per chat — Opus 5, Opus 4.8, Sonnet 5,
                Sonnet 4.6, Haiku 4.5, Fable 5 — or a family alias that follows
                the newest release</li>
            <li><b>Permission mode per chat</b>, straight from the toolbar, with
                the full set the CLI accepts</li>
            <li>Redesigned prompt composer: one rounded card that highlights on
                focus, with the image button, usage readout and Send inside it</li>
        </ul>

        <b>0.2.0</b>
        <ul>
            <li><b>Usage statistics</b> — token usage by project, git branch, model
                and day, read from the CLI's own session transcripts, plus usage
                since the current rate-limit window opened</li>
            <li>Redesigned chat panel: colours and font taken from the IDE theme,
                status dots instead of emoji, code blocks with a language header
                and copy button, collapsible tool output, and auto-scroll that
                stops following once you scroll up to read</li>
        </ul>

        <b>0.1.0</b>
        <ul>
            <li>Streaming chat with reasoning, rendered in an embedded browser
                (markdown + syntax highlighting), with a Swing fallback</li>
            <li>Tool activity with result status and collapsible output</li>
            <li>File edits reviewable in the native diff viewer, with per-file
                and whole-turn revert</li>
            <li>Persistent parallel sessions, usage analytics, permission modes,
                MCP server view, @-file references, image input, transcript export</li>
        </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>

    <!-- The embedded browser moved into its own bundled plugin in 2026.2; without
         this its classes aren't on our classpath at all. Optional because JCEF
         isn't shipped for every OS/architecture. -->
    <depends optional="true" config-file="claude-brains-jcef.xml">com.intellij.modules.jcef</depends>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="Claude Brains"
                    secondary="true"
                    icon="/icons/claude.svg"
                    anchor="right"
                    factoryClass="com.claudecode.chatplugin.ui.ChatToolWindowFactory"/>

        <!-- ClaudeSessionManager, ClaudeCliService and ClaudeCodeSettings are all
             annotated with @Service and are auto-registered as "light services" -
             no explicit <projectService> declaration needed on modern platforms. -->

        <projectConfigurable
            parentId="tools"
            id="com.claudecode.chatplugin.settings"
            displayName="Claude Brains"
            instance="com.claudecode.chatplugin.ui.ClaudeBrainsConfigurable"/>

        <notificationGroup id="Claude Brains" displayType="BALLOON"/>
    </extensions>

    <actions>
        <group id="ClaudeCodeChat.EditorPopup" text="Claude Brains" popup="true">
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>

            <action id="ClaudeCodeChat.SendSelection"
                    class="com.claudecode.chatplugin.actions.SendSelectionToClaudeAction"
                    text="Send Selection to Claude"
                    description="Send the highlighted code to the Claude Code chat panel"/>

            <action id="ClaudeCodeChat.SendFile"
                    class="com.claudecode.chatplugin.actions.SendFileToClaudeAction"
                    text="Reference This File in Chat"
                    description="Mention this file's path in the chat so Claude can read it itself"/>
        </group>

        <action id="ClaudeCodeChat.FloatingPrompt"
                class="com.claudecode.chatplugin.actions.FloatingPromptAction"
                text="Claude Code: Floating Prompt"
                description="Open a lightweight prompt popup near the caret">
            <keyboard-shortcut keymap="$default" first-keystroke="alt shift C"/>
        </action>

        <action id="ClaudeCodeChat.UsageStats"
                class="com.claudecode.chatplugin.actions.ShowUsageStatsAction"
                text="Claude Brains: Usage Statistics"
                description="Token usage by project, branch and model"/>

        <action id="ClaudeCodeChat.AcceptAllChanges"
                class="com.claudecode.chatplugin.actions.AcceptAllChangesAction"
                text="Accept All Claude Changes"
                description="Keep every change Claude made in this project"/>

        <action id="ClaudeCodeChat.RejectAllChanges"
                class="com.claudecode.chatplugin.actions.RejectAllChangesAction"
                text="Reject All Claude Changes"
                description="Restore every file Claude changed to how it was before"/>

        <action id="ClaudeCodeChat.RenameSession"
                class="com.claudecode.chatplugin.actions.RenameSessionAction"
                text="Claude Brains: Rename Chat"
                description="Give the open conversation a name you'll recognise"/>

        <action id="ClaudeCodeChat.DeleteSession"
                class="com.claudecode.chatplugin.actions.DeleteSessionAction"
                text="Claude Brains: Delete Chat"
                description="Delete the open conversation"/>

        <action id="ClaudeCodeChat.NewSession"
                class="com.claudecode.chatplugin.actions.NewSessionAction"
                text="Claude Code: New Chat Session"
                description="Start a new named Claude Code chat session"/>
    </actions>
</idea-plugin>
